#include <stdio.h>

int main() {
    int a = 9;
    int b = 10;
    int c = a+b;
    //printf("The value of a is %d value of b is %d and their sum is %d",a,b,c);
    printf("The reminder when a is divided by b is %d",a%b);
    //int d = a^b; doesn't work as power
    return 0;
}