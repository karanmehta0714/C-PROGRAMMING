#include <stdio.h>

int main() {
    int a = 2;
    float b = 6.8;
    printf("the value of a divide by b is %f \n",a/b);
    int k = 8.9;
    printf("the value of k is %d",k);//if any float number is intialize as int then it will be like this 8
    return 0;
}